import MobileMain from './Mobile';

const MainPage = () => {
	return (
		// <div>
		// 	<MenuBar></MenuBar>
		// 	<Preview
		// 		categoryData={1}
		// 		userLocation={'도곡동'}
		// 		userName={'이재훈'}
		// 	></Preview>
		// </div>
		<MobileMain></MobileMain>
	);
};

export default MainPage;
